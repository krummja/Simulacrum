#pragma once

#include "state.hpp"
#include <atomic>
#include <future>
#include <string>

namespace Simulacrum {

    class LoadingState : public State {
    public:
        LoadingState() = default;

    private:

    };

} // namespace Simulacrum
